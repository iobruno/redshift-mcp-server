# Copy this file to .env (git-ignored) and fill in real values.
# compose.yaml reads .env automatically for variable substitution.
AWS_PROFILE=
AWS_REGION=

# Optional -- compose.yaml already defaults these if left unset:
# REDSHIFT_MCP_PORT=8080
# REDSHIFT_MCP_WORKERS=4
# FASTMCP_LOG_LEVEL=INFO
